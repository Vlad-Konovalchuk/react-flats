import styled from 'styled-components'

const FlatBlock = styled.div`
  background-color:red;
`;

const FlatsList = styled.ul`
  margin:0;
`;
const FlatsListItem = styled.li`
  margin: 0;
  display: flex;
`;
export const Styled = {
    FlatBlock,
    FlatsList,
    FlatsListItem,
}
